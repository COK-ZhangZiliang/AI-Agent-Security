# Python environments / caches (local only, huge)
miniconda3/
envs/
.cache/
.conda_pkgs/
__pycache__/
*.pyc

# Kaggle credentials — NEVER commit
.kaggle/
**/access_token
**/kaggle.json

# Competition SDK (organizer-owned, not for redistribution)
comp_data/
comp_data.zip

# Third-party reference notebooks (other competitors' code)
ref_kernels/

# Kaggle kernel run outputs (regenerable)
kernel_out/
kernel_out2/
kernel_out3/
kernel_out*/
.kout*/

# Misc local artifacts
*.zip

# Token screenshot (contains credential)
image.png
.ext_kernels/

# Old SDK backup (kept locally for diffing only)
.comp_data_310_backup/

# Local pull-downs and old run artifacts
v4_out/
logs/
